{
  "name": "backlink-operations",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Human-in-loop compliant backlink operations with Playwright + Google Sheets + Admin UI.",
  "scripts": {
    "run": "node src/runner.js",
    "run:once": "node src/runner.js --limit=1",
    "ui:dev": "npm --prefix ui run dev",
    "ui:build": "npm --prefix ui run build",
    "ui:start": "npm --prefix ui run start"
  },
  "dependencies": {
    "googleapis": "^140.0.1",
    "playwright": "^1.50.1"
  }
}
